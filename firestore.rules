
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes to all documents.
      // This is insecure for production but useful for debugging.
      allow read, write: if true;
    }
  }
}
